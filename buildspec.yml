version: 0.1

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - images/*
    - styles/main.css
    - dist/bundle.js
