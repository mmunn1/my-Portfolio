version: 0.1

phase:
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - images/*
    - styles/main.css
    - dist/bundle.js
